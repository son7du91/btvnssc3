#include <stdio.h>
int main()
{
    float height, length, area;
    printf("Nhap chieu cao tam giác: "); scanf("%f",&height);
    printf("Nhap do dài canh dáy cua tam giác: ");scanf("%f",&length);
    printf("Dien tích tam giác là: %.2f", area=0.5*(height*length));
}
